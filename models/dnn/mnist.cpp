#include "read_idx.hpp"
#include "../../nnet_toolkit/primitives.hpp"

int main() {

    /*
    auto trainingImages = new idx::Set<unsigned char>("train-images.idx3-ubyte");
    auto trainingLabels = new idx::Set<unsigned char>("train-labels.idx1-ubyte");

    auto dims = trainingImages->dims();

    uint32_t I, R, C;

    I = std::get<0>(dims);
    R = std::get<1>(dims);
    C = std::get<2>(dims);

    std::cout << I << ", " << R << ", " << C << std::endl; 
    */

   


    return 0;

}